---
phase: 02-card-view-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/store/wallet-store.ts
  - components/sections/yield-section/types.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "View mode persists across page refreshes"
    - "View mode state is accessible from yield section"
    - "Default view mode is 'list' for backwards compatibility"
  artifacts:
    - path: "lib/store/wallet-store.ts"
      provides: "Yield view mode state with persistence"
      contains: "yieldViewMode"
      exports: ["yieldViewMode", "setYieldViewMode"]
    - path: "components/sections/yield-section/types.ts"
      provides: "View mode type definition"
      contains: "YieldViewMode"
  key_links:
    - from: "components/sections/yield-section/yield-section.tsx"
      to: "lib/store/wallet-store.ts"
      via: "useWalletStore selector"
      pattern: "useWalletStore\\(state => state\\.yieldViewMode\\)"
---

<objective>
Add view mode state to wallet store with persistence support.

Purpose: Enable users to switch between list and card views for yield opportunities, with preference persisting across browser refreshes.
Output: Extended WalletState interface with yieldViewMode field, persisted via Zustand middleware.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-card-view-implementation/02-RESEARCH.md

# Phase 1 research patterns
View mode state pattern from NFT section:
- Uses local useState without persistence
- For yield section, we use Zustand store for persistence

# Current wallet store structure
lib/store/wallet-store.ts
- Already has persist middleware configured
- partialize function defines what gets persisted (wallets, selectedWalletId, privacyMode)
- Need to add yieldViewMode to state and partialize
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add YieldViewMode type to types.ts</name>
  <files>components/sections/yield-section/types.ts</files>
  <action>
    Add type definition to components/sections/yield-section/types.ts:

    ```typescript
    /**
     * View mode for yield section display
     */
    export type YieldViewMode = 'list' | 'card';
    ```

    Add this after the YieldCategoryFilter type definition (around line 82).
  </action>
  <verify>grep -n "YieldViewMode" components/sections/yield-section/types.ts</verify>
  <done>YieldViewMode type exists with 'list' | 'card' union</done>
</task>

<task type="auto">
  <name>Task 2: Extend WalletState with view mode properties</name>
  <files>lib/store/wallet-store.ts</files>
  <action>
    Modify lib/store/wallet-store.ts:

    1. Add to WalletState interface (after line 121, before syncTrigger):
    ```typescript
    // Yield section view mode
    yieldViewMode: 'list' | 'card';
    setYieldViewMode: (mode: 'list' | 'card') => void;
    ```

    2. Add initial state in create() call (after line 155, after privacyMode):
    ```typescript
    yieldViewMode: 'list',
    ```

    3. Add action implementation (after togglePrivacyMode, around line 409):
    ```typescript
    setYieldViewMode: (mode: 'list' | 'card') => {
      set({ yieldViewMode: mode });
    },
    ```

    4. Add to persist partialize (line 599-603):
    ```typescript
    partialize: (state) => ({
      wallets: state.wallets,
      selectedWalletId: state.selectedWalletId,
      privacyMode: state.privacyMode,
      yieldViewMode: state.yieldViewMode,
    }),
    ```

    Do NOT modify any other parts of the store. Maintain existing patterns.
  </action>
  <verify>grep -n "yieldViewMode" lib/store/wallet-store.ts | wc -l</verify>
  <done>4 matches found (interface, initial state, action, partialize)</done>
</task>

</tasks>

<verification>
1. Check TypeScript compilation: `npm run type-check`
2. Verify yieldViewMode is typed correctly
3. Confirm persistence includes yieldViewMode
</verification>

<success_criteria>
- yieldViewMode property added to WalletState interface
- setYieldViewMode action added to store
- Initial value set to 'list' for backwards compatibility
- yieldViewMode included in persist partialize function
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-card-view-implementation/02-01-SUMMARY.md`
</output>
